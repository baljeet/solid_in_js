class User {
  constructor(name, email) {
    this.name = name;
    this.email = email;
  }
}

class Authenticator {
  authenticate(user, password) {
    // code to authenticate user
  }
}

class Logger {
  logAccess(user) {
    // code to log access
  }
}

class Validator {
  validateData(user) {
    // code to validate user data
  }
}
